# Salin file ini menjadi secrets.toml lalu isi dengan kredensial admin Anda sendiri.
# Untuk membuat hash password baru, jalankan di terminal:
#   python -c "import hashlib; print(hashlib.sha256('PASSWORD_BARU_ANDA'.encode()).hexdigest())"

admin_username = "admin"
admin_password_hash = "ganti_dengan_hasil_hash_sha256_anda"
